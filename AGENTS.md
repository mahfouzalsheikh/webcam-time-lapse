# Development workflow

After making changes to this app, run `./rebuild-and-restart.sh` and verify that the service is healthy before reporting completion. The user wants each completed update rebuilt and restarted automatically.
