# Select your webcam in the web interface under Capture settings.
# The rebuild script discovers connected webcams and their permission groups.
PORT=8091
# Local access by default. Use 0.0.0.0 only on a trusted LAN.
BIND_ADDRESS=127.0.0.1
